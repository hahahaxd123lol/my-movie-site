FLIX2WATCH v86 — WHITE ROLE PARTICLES FOREVER

NO SQL NEEDED.
NO EDGE FUNCTION REDEPLOY NEEDED.

SITE-WIDE RULE
- Display-name LETTERS use the colour of the account's highest role.
- Particle GIF is forced WHITE regardless of role.
- Particles sit slightly outside the whole word so they visibly fly around
  the letters instead of being crushed directly onto each character.
- Particle area is still bound to the display-name width, so it cannot become
  a wide banner across autocomplete, rankings, chat, comments, or leaderboard.
- Search Users autocomplete gets the same white word-bound particle layer.
- Old secondary/pseudo/fairy-dust layers remain disabled.

Everything from cumulative v85 is preserved.

f2w-force-save:readme-v86:1788226708
 