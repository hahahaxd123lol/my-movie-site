FLIX2WATCH v83 — DISPLAY NAME ROLE COLOUR + TIGHT WHITE PARTICLES

NO SQL NEEDED.
NO EDGE FUNCTION REDEPLOY NEEDED.

FIXED
- Role display names keep the colour of their highest role.
- A stale inline white style is now removed when role data resolves, so Owner
  cannot stay white after initially rendering as a no-role name.
- White Particles.gif is attached directly to the display-name text box.
- Pseudo particle layers are disabled, eliminating wide banner effects.
- Search Users autocomplete now separates:
    display name  <- role colour + white particles
    @username     <- normal text
    View public profile <- normal text
- The particle effect cannot span the autocomplete row/card anymore.
- Compact forum/leaderboard/chat/comment names remain text-width only.

Everything else from cumulative v82 is preserved.

f2w-force-save:readme-v83:1788226300
 