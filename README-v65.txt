FLIX2WATCH v65 — SEARCH INPUT HARD FIX + USER RESULTS PAGE

NO SQL NEEDED.
NO EDGE FUNCTION REDEPLOY NEEDED.

SEARCH FIELDS
- Site-wide search inputs are normal search boxes, not credential boxes.
- Search fields start readonly and unlock only on real pointer interaction.
- Randomized non-credential field names are used.
- Password-manager ignore hints remain.
- Closed auth modal is marked inert so hidden login fields are not associated
  with unrelated search inputs.
- Login/Signup immediately removes inert from the auth modal.

USERS SEARCH PAGE
- Result cards are larger and easier to read.
- Avatar, username and metadata are increased moderately, not oversized.
- Results are capped at 30 users per page.
- Numeric pagination is styled at the bottom: 1, 2, 3, 4, 5 with prev/next.

f2w-force-save:readme-v65:1788222474
 