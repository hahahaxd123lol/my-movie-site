(()=>{
'use strict';
if(window.__f2wV166Notifications)return;window.__f2wV166Notifications=true;
const URL='https://viqufxlcxwgboyxbdhjb.supabase.co',KEY='sb_publishable_zdfvnwwgL9LI3yTK0-1Sbg_RsYRvNge';
let client=null,channel=null,timer=0,uid='',open=false,loading=false,rowsCache=[];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const rel=v=>{const t=new Date(v).getTime(),d=Math.max(0,Date.now()-t);if(!Number.isFinite(t))return'';if(d<60000)return'just now';if(d<3600000)return Math.floor(d/60000)+'m ago';if(d<86400000)return Math.floor(d/3600000)+'h ago';return Math.floor(d/86400000)+'d ago'};
function db(){return client||(client=window.f2wSupabase||window.chatSupabase||window.supabaseClient||(window.supabase?.createClient?window.supabase.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}):null))}
function E(){return{wrap:document.getElementById('notification-wrap'),btn:document.getElementById('notification-btn'),menu:document.getElementById('notification-menu'),list:document.getElementById('notification-list'),count:document.getElementById('notification-count')}}
function setOpen(v){open=!!v;const{menu,btn}=E();if(menu){menu.hidden=!open;menu.style.setProperty('display',open?'block':'none','important');menu.setAttribute('aria-hidden',open?'false':'true')}if(btn)btn.setAttribute('aria-expanded',open?'true':'false')}
function render(rows){rowsCache=Array.isArray(rows)?rows:rowsCache;const{list,count}=E();if(!list)return;const unread=rowsCache.filter(n=>!n.read_at).length;if(count){count.textContent=String(unread);count.hidden=unread===0}list.innerHTML=rowsCache.length?rowsCache.map(n=>`<a class="f2w-v166-notification ${n.read_at?'read':'unread'}" href="${esc(n.link||'#')}"><strong>${esc(n.title||'Notification')}</strong><span>${esc(n.message||'')}</span><small>${esc(rel(n.created_at))}</small></a>`).join(''):'<div class="v17-notification-empty">No notifications yet.</div>'}
async function load({silent=false}={}){if(loading)return;const c=db(),{list}=E();if(!c?.rpc||!list)return;loading=true;try{let out=await c.rpc('get_my_notifications_v166',{p_limit:60});if(out?.error?.code==='PGRST202'||out?.error?.code==='42883')out=await c.rpc('get_my_notifications_v161',{p_limit:60});if(out?.error)throw out.error;render(Array.isArray(out.data)?out.data:[])}catch(e){console.warn('v166 notifications load',e);if(!silent&&!rowsCache.length)list.innerHTML='<div class="v17-notification-empty">Could not load notifications. Click Notifications to retry.</div>'}finally{loading=false;if(open)setOpen(true)}}
async function markAll(){const c=db(),{menu}=E();if(!c?.rpc)return;try{let out=await c.rpc('mark_my_notifications_read_v166');if(out?.error?.code==='PGRST202'||out?.error?.code==='42883')out=await c.rpc('mark_my_notifications_read_v161');if(out?.error)throw out.error;rowsCache=rowsCache.map(n=>({...n,read_at:n.read_at||new Date().toISOString()}));render(rowsCache);setOpen(true);await load({silent:true})}catch(e){console.warn('v166 mark all read failed',e);if(menu){menu.dataset.error='1';setTimeout(()=>delete menu.dataset.error,1800)}}}
async function currentSession(){try{return(await db()?.auth?.getSession())?.data?.session||null}catch{return null}}
async function bind(newUid){const c=db();if(!c)return;if(channel){try{c.removeChannel(channel)}catch{}channel=null}clearInterval(timer);uid=newUid||'';if(!uid)return;try{channel=c.channel('f2w-v166-notify-'+uid).on('postgres_changes',{event:'*',schema:'public',table:'f2w_notifications_v125',filter:`user_id=eq.${uid}`},()=>load({silent:true})).subscribe()}catch(e){console.warn('v166 realtime',e)}timer=setInterval(()=>{if(document.visibilityState==='visible'&&open)load({silent:true})},30000);load({silent:true})}
async function sync(){const s=await currentSession(),newUid=s?.user?.id||'',logged=!!newUid,{wrap}=E();if(wrap){wrap.hidden=!logged;wrap.style.display=logged?'block':'none'}if(!logged){setOpen(false);rowsCache=[];render([])}if(newUid!==uid)await bind(newUid)}
function wire(){document.querySelectorAll('#notification-btn').forEach(b=>b.removeAttribute('onclick'));document.querySelectorAll('#notification-menu .v17-notification-head button').forEach(b=>b.removeAttribute('onclick'));
 document.addEventListener('click',e=>{const b=e.target.closest?.('#notification-btn');if(b){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();setOpen(!open);if(open)load();return}const m=e.target.closest?.('#notification-menu .v17-notification-head button');if(m){e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();markAll();return}if(open&&!e.target.closest?.('#notification-wrap'))setOpen(false)},true);
 document.addEventListener('keydown',e=>{if(e.key==='Escape'&&open)setOpen(false)},true);
 window.toggleNotifications=e=>{e?.preventDefault?.();e?.stopPropagation?.();setOpen(!open);if(open)load()};window.markAllNotificationsRead=markAll;
 const mo=new MutationObserver(()=>{const{menu}=E();if(open&&menu&&(menu.hidden||getComputedStyle(menu).display==='none'))setOpen(true)});mo.observe(document.documentElement,{subtree:true,attributes:true,attributeFilter:['hidden','style','class']});
}
async function boot(){wire();await sync();db()?.auth?.onAuthStateChange?.((_e,s)=>{const newUid=s?.user?.id||'';const{wrap}=E();if(wrap){wrap.hidden=!newUid;wrap.style.display=newUid?'block':'none'}if(newUid!==uid)bind(newUid);if(!newUid)setOpen(false)});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'){sync();if(open)load({silent:true})}},{passive:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
